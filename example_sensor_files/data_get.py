# -*- condig: utf-8 -*-

import smbus
import time
import RPi.GPIO as GPIO

class Sensor(object):

  def __init__():

  def get_sensor_value():
      self.value = 0
      return value

  def pooling_get_value(array_method):
      while roop_flag:
         self.sensor_value = get_sensor_value()
         if self.sensor_value:
             for call_method in array_method:
                  call_method()

         else:
             time.sleep(0.2)

  def period_get_valu():
      while roop_flag:
          return get_sensor_value()

  def exception_method():
      # 例外処理を記述

  def set_in():
# 入力用のpin番号をセットするためのメソッド
# in_pinがnullの場合、動かないようにする。

  def set_out():
# 出力用のpin番号をセットするためのメソッド

  def set_cycle():
# 周期を定義する変数だが、そもそもすべてwhileで回すと決めていいのか?
# forで20回だけセンシングするといった場合はないのか？
# と、おもったけどforで回しても周期あるわ〜

  def exception_method():
      # 例外処理を記述
